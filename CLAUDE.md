# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Source of Truth

Read from: `main/`, `components/bsp_extra/`, and the latest handoff docs in `HANDOFF_FOLDER/`.

**Never read or list these directories** (large, not source of truth, wastes quota):
- `build/`
- `docs/`
- `docs_backup/`
- `UI/`
- `claudetransferv2/`
- `gui squareline files/`
- `HANDOFF_FOLDER/` session summaries older than the latest

If a task seems to require reading one of the above, ask first.
